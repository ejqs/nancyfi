{
  "name": "Nancyfi",
  "install": "bash .cursor/dev-install.sh",
  "start": "bash .cursor/dev-start.sh",
  "terminals": [
    {
      "name": "next-dev",
      "command": "export PATH=\"$HOME/.bun/bin:$PATH\" && export DATABASE_URL=\"postgresql://nancyfi:nancyfi@127.0.0.1:5432/nancyfi\" && bash .cursor/dev-start.sh && bun run dev",
      "description": "Next.js dev server on http://localhost:3000 (uses the local Postgres)"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
